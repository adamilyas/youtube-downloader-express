{
	"name": "youtube-downloader-typescript-express",
	"version": "1.0.0",
	"description": "This projects can be used as template for a typescript-express backend",
	"main": "src/index.js",
	"scripts": {
		"prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
		"build": "tsc",
		"prestart": "npm run build",
		"start": "node .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "adam",
	"license": "ISC",
	"dependencies": {
		"@types/express": "^4.17.6",
		"express": "^4.17.1",
		"typescript": "^3.9.3",
		"ytdl-core": "^2.1.4"
	},
	"devDependencies": {
		"@types/node": "^14.0.5",
		"tslint": "^6.1.2"
	}
}
